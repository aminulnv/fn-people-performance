# Dependencies
node_modules

# Build
dist
*.local

# Vercel
.vercel

# Env (keep examples tracked)
.env
.env.local
.env.deploy
.env.*.local
!.env.example
!.env.deploy.example
!.env.platform.example

# ClickUp task log (local tracking, not for repo)
conext_docs/clickup-project-tasks.md

# Local HR source data / generated PII (never publish)
*.csv
db/migrations/*_seed*.sql
Screenshot*.png

# Editor / OS
.DS_Store
Thumbs.db
*.log
*.tsbuildinfo

# Platform API runtime (CA fetched at deploy; deps installed in Docker)
server/certs/
server/node_modules/
