/** Build-time app version from package.json (see vite.config.ts). */
export const APP_VERSION = __APP_VERSION__

/** Display label for UI and bug-report screenshots. */
export const APP_VERSION_LABEL = `v${APP_VERSION}`
